package controller.components;

public class UserNamePasswordVerifier {

    /*
        --> This class will verify the user name and password that is
            provided by the user.

            @implementation
            new UserNamePasswordVerifier.verify(username,password);

            @returns
            if validated -> true;
     */

    public boolean verify(String userName, String password){
        return true;
    }
}
